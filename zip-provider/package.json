{
  "name": "zip-provider",
  "version": "0.1.6",
  "publisher": "dotdir",
  "displayName": "ZIP Provider",
  "description": "Browse ZIP archive contents for .dir",
  "scripts": {
    "build": "cargo build --target wasm32-unknown-unknown --release && mkdir -p dist && cp target/wasm32-unknown-unknown/release/zip_provider.wasm dist/provider.wasm",
    "dev": "cargo build --target wasm32-unknown-unknown && mkdir -p dist && cp target/wasm32-unknown-unknown/debug/zip_provider.wasm dist/provider.wasm"
  },
  "contributes": {
    "fsProviders": [
      {
        "id": "zip-provider",
        "label": "ZIP Provider",
        "patterns": [
          "*.zip"
        ],
        "entry": "dist/provider.wasm",
        "runtime": "backend",
        "priority": 10
      }
    ]
  }
}
